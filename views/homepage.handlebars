{{#each posts as |post| }}
<div class="">
  <div class="">
    <a href="/post/{{post.id}}">
      <h3>{{post.title}}</h3>
      <h5>Posted by {{user.username}} on {{format_date post.date_created}}</h5>
      <p>{{post.content}}</p>
    </a>
  </div>
</div>
{{/each}}
